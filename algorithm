# 시간 복잡도: O(nt)
# 공간 복잡도: O(n)
# 사용한 알고리즘: 동적 계획법, Top-Down
# 사용한 자료구조: 1차원 배열

t = int(input())
d = [0] * 12


def go(i):
    if i == 0: return 1

    if d[i] > 0: return d[i]

    for j in range(1, 4):
        if i - j >= 0:
            d[i] += go(i - j)

    return d[i]


while t > 0:
    t -= 1
    n = int(input())

    print(go(n))
