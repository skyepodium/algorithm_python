# 시간 복잡도: O(nk)
# 공간 복잡도: O(n)
# 사용한 알고리즘: 동적 계획법, Bottom-Up
# 사용한 자료구조: 1차원 배열

a = [0]*101
d = [10001] * 10001

n, k = map(int, input().split())

for i in range(n):
    a[i] = int(input())

d[0] = 0
for i in range(n):
    for j in range(k+1):
        if j - a[i] >= 0:
            d[j] = min(d[j], d[j-a[i]]+1)

if d[k] == 10001:
    d[k] = -1

print(d[k])
