import sys
input = sys.stdin.readline
print = sys.stdout.write


def gcd(a, b):
    if b == 0:
        return a
    else:
        return gcd(b, a % b)


a, b = map(int, input().split())
print("%d\n" % gcd(a, b))
print("%d\n" % ((a*b)/gcd(a, b)))