import sys
input = sys.stdin.readline
print = sys.stdout.write

d = []
n = int(input())
for _ in range(n):
    a, b = map(int, input().split())
    d.append((b, a))

for a, b in sorted(d):
    print("%d %d\n" % (b, a))